# Infrastructure

Infrastructure as Code, Kubernetes manifests, and Docker Compose files will live here.

Planned structure:
- `docker/` — Dockerfiles
- `kubernetes/` — K8s manifests
- `terraform/` — Cloud provisioning
- `helm/` — Helm charts
