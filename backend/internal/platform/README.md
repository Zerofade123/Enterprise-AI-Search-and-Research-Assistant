# Shared Platform (Go)

Shared configuration, logging, errors, and validation live in `internal/platform`.

## Modules

- `config` — environment-based configuration loader
- `logging` — structured JSON logging (zap)
- `errors` — typed application errors
- `validation` — centralized validation utilities
