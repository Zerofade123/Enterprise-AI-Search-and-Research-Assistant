# AI Services (Python)

This directory contains AI/ML services implemented with FastAPI.

Planned services:
- Document Processor
- Embedding Service
- RAG Service
- Agent Service

Each service will have:
- `app/` domain + pipelines
- `tests/` unit and integration tests
- Dockerfile for deployment
